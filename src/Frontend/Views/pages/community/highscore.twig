{% extends "Frontend/Views/layouts/app.twig" %}
{% block body %}
<div class="bg-lush-content-bg d-flex flex-column h-100">
  <div class="py-4">
    <div class="mb-15p pt-15p">
      <div class="row">
        {% if credits is empty %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-credits">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium">
                </div>
              </div>
              <div class="b-overlay position-absolute" style="inset: 0px; z-index: 10;">
                <div class="position-absolute bg-dark rounded-sm" style="inset: 0px; opacity: 0.85; backdrop-filter: blur(2px);"></div>
                  <div class="position-absolute" style="top: 50%; left: 50%; transform: translateX(-50%) translateY(-50%);">
                    <b class="w-100 text-center d-block my-auto text-white">{{ lang('website.ranking.contestants_empty' )}}</b>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% else %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-credits">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium"> 
                  {% for credits in credits|slice(0, 3) %}
                   <img src="{{  config('website_settings.site.fpath') }}?figure={{credits.look}}&head_direction=3" class="btn btn-link p-0">
                  {% endfor %}
                </div>
              </div>
              <div class="leader-sub small px-2 py-2">{{ lang('website.ranking.most_credits' )}}</div>
              <ul class="list-group">
                {% for credits in credits %}
                 <li class="list-group-item d-flex flex-row">
                  <div class="d-flex flex-row text-muted small px-2 py-2 justify-content-end w-100">
                    <div class="d-flex flex-row">
                      <span class="pr-1">#{{ loop.index }}</span>
                      <span class="align-self-center">
                        <a href="/home/{{credits.username}}">
                          <span class="text-lush-info-text">{{credits.username}}</span>
                        </a>
                      </span>
                    </div>
                    <span class="align-self-end w-100 text-right">{{credits.credits}}</span>
                  </div>
                </li> 
                {% endfor %}
               </ul>
            </div>
          </div>
        </div>
        {% endif %}
        {% if duckets is empty %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-duckets">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium">
                </div>
              </div>
              <div class="b-overlay position-absolute" style="inset: 0px; z-index: 10;">
                <div class="position-absolute bg-dark rounded-sm" style="inset: 0px; opacity: 0.85; backdrop-filter: blur(2px);"></div>
                  <div class="position-absolute" style="top: 50%; left: 50%; transform: translateX(-50%) translateY(-50%);">
                    <b class="w-100 text-center d-block my-auto text-white">{{ lang('website.ranking.contestants_empty' )}}</b>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% else %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-duckets">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium"> 
                  {% for duckets in duckets|slice(0, 3) %}
                   <img src="{{  config('website_settings.site.fpath') }}?figure={{duckets.user.look}}&head_direction=3" class="btn btn-link p-0">
                  {% endfor %}
                </div>
              </div>
              <div class="leader-sub small px-2 py-2">{{ lang('website.ranking.most_duckets' )}}</div>
              <ul class="list-group">
                {% for duckets in duckets %}
                 <li class="list-group-item d-flex flex-row">
                  <div class="d-flex flex-row text-muted small px-2 py-2 justify-content-end w-100">
                    <div class="d-flex flex-row">
                      <span class="pr-1">#{{ loop.index }}</span>
                      <span class="align-self-center">
                        <a href="/home/{{duckets.user.username}}">
                          <span class="text-lush-info-text">{{duckets.user.username}}</span>
                        </a>
                      </span>
                    </div>
                    <span class="align-self-end w-100 text-right">{{duckets.amount}}</span>
                  </div>
                </li> 
                {% endfor %}
               </ul>
            </div>
          </div>
        </div>
        {% endif %}
        {% if diamonds is empty %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-diamonds">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium">
                </div>
              </div>
              <div class="b-overlay position-absolute" style="inset: 0px; z-index: 10;">
                <div class="position-absolute bg-dark rounded-sm" style="inset: 0px; opacity: 0.85; backdrop-filter: blur(2px);"></div>
                  <div class="position-absolute" style="top: 50%; left: 50%; transform: translateX(-50%) translateY(-50%);">
                    <b class="w-100 text-center d-block my-auto text-white">{{ lang('website.ranking.contestants_empty' )}}</b>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% else %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-diamonds">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium"> 
                  {% for diamond in diamonds|slice(0, 3) %}
                   <img src="{{  config('website_settings.site.fpath') }}?figure={{diamond.user.look}}&head_direction=3" class="btn btn-link p-0">
                  {% endfor %}
                </div>
              </div>
              <div class="leader-sub small px-2 py-2">{{ lang('website.ranking.most_duckets' )}}</div>
              <ul class="list-group">
                {% for diamond in diamonds %}
                 <li class="list-group-item d-flex flex-row">
                  <div class="d-flex flex-row text-muted small px-2 py-2 justify-content-end w-100">
                    <div class="d-flex flex-row">
                      <span class="pr-1">#{{ loop.index }}</span>
                      <span class="align-self-center">
                        <a href="/home/{{diamond.user.username}}">
                          <span class="text-lush-info-text">{{diamond.user.username}}</span>
                        </a>
                      </span>
                    </div>
                    <span class="align-self-end w-100 text-right">{{diamond.amount}}</span>
                  </div>
                </li> 
                {% endfor %}
               </ul>
            </div>
          </div>
        </div>
        {% endif %}
        {% if achievements is empty %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-score">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium">
                </div>
              </div>
              <div class="b-overlay position-absolute" style="inset: 0px; z-index: 10;">
                <div class="position-absolute bg-dark rounded-sm" style="inset: 0px; opacity: 0.85; backdrop-filter: blur(2px);"></div>
                  <div class="position-absolute" style="top: 50%; left: 50%; transform: translateX(-50%) translateY(-50%);">
                    <b class="w-100 text-center d-block my-auto text-white">{{ lang('website.ranking.contestants_empty' )}}</b>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% else %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-score">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium"> 
                  {% for achievements in achievements|slice(0, 3) %}
                    <img src="{{  config('website_settings.site.fpath') }}?figure={{achievements.user.look}}&head_direction=3" class="btn btn-link p-0">
                  {% endfor %}
                </div>
              </div>
              <div class="leader-sub small px-2 py-2">{{ lang('website.ranking.most_ap' )}}</div>
              <ul class="list-group">
                {% for achievements in achievements %}
                <li class="list-group-item d-flex flex-row">
                  <div class="d-flex flex-row text-muted small px-2 py-2 justify-content-end w-100">
                    <div class="d-flex flex-row">
                      <span class="pr-1">#{{ loop.index }}</span>
                      <span class="align-self-center">
                        <a href="/home/{{achievements.user.username}}">
                          <span class="text-lush-info-text">{{achievements.user.username}}</span>
                        </a>
                      </span>
                    </div>
                    <span class="align-self-end w-100 text-right">{{achievements.achievement_score}}</span>
                  </div>
                </li> 
                {% endfor %}
               </ul>
            </div>
          </div>
        </div>
        {% endif %}
        {% if respects is empty %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-respect">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium">
                </div>
              </div>
              <div class="b-overlay position-absolute" style="inset: 0px; z-index: 10;">
                <div class="position-absolute bg-dark rounded-sm" style="inset: 0px; opacity: 0.85; backdrop-filter: blur(2px);"></div>
                  <div class="position-absolute" style="top: 50%; left: 50%; transform: translateX(-50%) translateY(-50%);">
                    <b class="w-100 text-center d-block my-auto text-white">{{ lang('website.ranking.contestants_empty' )}}</b>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% else %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-respect">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium"> 
                {% for respects in respects|slice(0, 3) %}
                  <img src="{{  config('website_settings.site.fpath') }}?figure={{respects.user.look}}&head_direction=3" class="btn btn-link p-0"> 
                {% endfor %}
                </div>
              </div>
              <div class="leader-sub small px-2 py-2">{{ lang('website.ranking.most_respect' )}}</div>
              <ul class="list-group">
                {% for respects in respects %}
                <li class="list-group-item d-flex flex-row">
                  <div class="d-flex flex-row text-muted small px-2 py-2 justify-content-end w-100">
                    <div class="d-flex flex-row">
                      <span class="pr-1">#{{ loop.index }}</span>
                      <span class="align-self-center">
                        <a href="/home/{{respects.user.username}}">
                          <span class="text-lush-info-text">{{respects.user.username}}</span>
                        </a>
                      </span>
                    </div>
                    <span class="align-self-end w-100 text-right">{{respects.respects_received}}</span>
                  </div>
                </li> 
                {% endfor %}
               </ul>
            </div>
          </div>
        </div>
        {% endif %}
        {% if online is empty %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-time">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium">
                </div>
              </div>
              <div class="b-overlay position-absolute" style="inset: 0px; z-index: 10;">
                <div class="position-absolute bg-dark rounded-sm" style="inset: 0px; opacity: 0.85; backdrop-filter: blur(2px);"></div>
                  <div class="position-absolute" style="top: 50%; left: 50%; transform: translateX(-50%) translateY(-50%);">
                    <b class="w-100 text-center d-block my-auto text-white">{{ lang('website.ranking.contestants_empty' )}}</b>
                  </div>
                </div>
              </div>
            </div>
          </div>
        {% else %}
        <div class="leaderboard col-md-1 mb-15p">
          <div class="card d-flex flex-column h-100 leaderboard-time">
            <div class="b-overlay-wrap position-relative h-100">
              <div class="leader-heading d-flex flex-column justify-content-end">
                <div class="podium"> 
                  {% for online in online|slice(0, 3) %}
                   <img src="{{  config('website_settings.site.fpath') }}?figure={{online.user.look}}&head_direction=3" class="btn btn-link p-0"> 
                  {% endfor %}
                </div>
              </div>
              <div class="leader-sub small px-2 py-2">{{ lang('website.ranking.most_online' )}}</div>
              <ul class="list-group">
                {% for online in online  %}
                <li class="list-group-item d-flex flex-row">
                  <div class="d-flex flex-row text-muted small px-2 py-2 justify-content-end w-100">
                    <div class="d-flex flex-row">
                      <span class="pr-1">#{{ loop.index }}</span>
                      <span class="align-self-center">
                        <a href="/home/{{online.user.username}}">
                          <span class="text-lush-info-text">{{online.user.username}}</span>
                        </a>
                      </span>
                    </div>
                    <span class="align-self-end w-100 text-right">{{ online.progress / 60|round }}h</span>
                  </div>
                </li> 
                {% endfor %}
               </ul>
            </div>
          </div>
        </div>
        {% endif %}
      </div>
    </div>
  </div>
</div>
{% endblock %}